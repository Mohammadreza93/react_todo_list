import React from 'react';
import {Typography} from "@mui/material";

const About = () => {
    return (
        <div>
            <Typography variant={'h5'} sx={{p: 2}}>About page</Typography>
        </div>
    );
};

export default About;